# Création d'un projet node.JS
# => ouvrez un terminal pointant sur le dossier cible
# Saisir la commande suivante
$ npm init

# Ré-installer l'ensemble des dépendances en une commande
$ npm install

# Import de Express
$ npm install express ejs

# Import de IP
$ npm install ip

# Définition du fonctionnement modulaire dans la fichier package.json
"type":"module",

# Installation du middleware nodemon
$ npm install nodemon --save-dev

